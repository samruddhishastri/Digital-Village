import 'package:flutter/material.dart';
import '../../Screens/Dashboard_admin/components/body.dart';

class DashboardAdmin extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
